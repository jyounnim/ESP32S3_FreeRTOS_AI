/*
 * PLACEHOLDER - replace this file with the real anomaly_model.h generated in
 * Step 3 of the KR doc (`xxd -i anomaly_model.tflite > anomaly_model.h`,
 * after training in lab/02_train_autoencoder/).
 *
 * This placeholder is intentionally left without real weights so the build
 * fails loudly (undefined symbol) if you forget to swap it in, rather than
 * silently running on garbage data.
 */

#pragma once

extern unsigned char anomaly_model_tflite[];
extern unsigned int anomaly_model_tflite_len;
